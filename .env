VITE_BOAT_API_URL=https://localhost:8443
# Customer-facing web app — used for admin → yacht-detail preview links
# ("View on customer site" button in the Create Reservation modal).
VITE_CUSTOMER_WEB_URL=http://localhost:3000
